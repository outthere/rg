<ul>
  <% if Admin::EnrollmentsController.searchable? %>
    <li>
      <%= render :partial => "/shared/admin/search",
                  :locals => {
                    :url => admin_enrollments_path
                  } %>
    </li>
  <% end %>
  <li>
    <%= link_to t('.create_new'), new_admin_enrollment_path,
                 :class => "add_icon" %>
  </li>
<% if !searching? and Admin::EnrollmentsController.sortable? and Enrollment.count > 1 %>
  <li>
    <%= link_to t('.reorder', :what => "Enrollments"),
                 admin_enrollments_path,
                 :id => "reorder_action",
                 :class => "reorder_icon" %>

    <%= link_to t('.reorder_done', :what => "Enrollments"),
                 admin_enrollments_path,
                 :id => "reorder_action_done",
                 :style => "display: none;",
                 :class => "reorder_icon" %>
  </li>
<% end %>
</ul>