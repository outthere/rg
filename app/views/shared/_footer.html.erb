<div class="wrap foo">
	<footer>

      <%= render :partial => "/shared/footer_menu", :locals => {
        :roots => @menu_pages.select{|p| p.parent_id == Page.find_by_constant_name('bottom_menu').id}
      } %>

      	<a class="logo" href="/<%= locale.to_s + '/'  if locale != RefinerySetting.find_by_name( :i18n_translation_current_locale ).value %>"></a>
		<p>Copyright <%= Time.now.year -%></p>
	</footer>
</div>
